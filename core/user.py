# coding = UTF8

class User:
    def __init__(filter):
        """
        通过所给的条件返回用户实体
        """

        # if channel is None:
        #     self.name = id
        # else:
        #     self.name = getUser(id, channel)
